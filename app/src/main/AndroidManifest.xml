<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tim.kotlinlearning">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher_share"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_share_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".Activities.Main2Activity">
        </activity>
        <activity android:name=".Activities.HobiesActivity">
        </activity>
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>